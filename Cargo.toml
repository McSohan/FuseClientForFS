[package]
name = "fuse_client_for_fs"
version = "0.1.0"
edition = "2024"



[dependencies]
libc = "0.2"
chrono = "0.4"
redox-scheme = "0.8.2"
bytemuck = { version = "1", features = ["derive"] }

[features]
virtio-fs = []
